'use strict';

// Place Mongoose model definitions here.
// Example:
//   const mongoose = require('mongoose');
//   const schema = new mongoose.Schema({ name: String });
//   module.exports = mongoose.model('Example', schema);
